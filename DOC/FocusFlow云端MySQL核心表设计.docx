FocusFlow 云端 MySQL 核心表设计



一、移动端用户主表 (biz_user)

存储用户账号信息及核心资产。支持标准的账号密码注册与登录。

user_id(BIGINT, 主键, 自增)：用户ID（主键，自增）

account(VARCHAR(32), 唯一索引)：登录账号（唯一）

password(VARCHAR(64))：密码（SHA-256加盐哈希值）

nickname(VARCHAR(64))：用户昵称（默认如 Focus_8848）

avatar_id(INT)：预设头像ID，默认值为 1

time_flux(INT)：光流余额（核心货币资产），必须以云端为准

streak_days(INT)：连续专注天数

last_focus_date(VARCHAR(10))：最后专注日期（YYYY-MM-DD格式）

last_focus_time(BIGINT)：最后专注时间戳（毫秒），用于花园点亮状态判断。用户专注完成时更新，好友充能时也会更新，实现统一的花园活力管理。

status(TINYINT)：账号状态（0=正常，1=封禁）

created_at(BIGINT)：注册时间戳（毫秒）

updated_at(BIGINT)：最后更新时间戳（毫秒）

deleted(TINYINT)：逻辑删除标记（0=未删除，1=已删除）



二、全局专注记录表 (biz_focus_record)

接收从Android端Room数据库同步上来的专注记录，用于后台数据大屏统计和用户多设备漫游。

record_id(CHAR(36), 主键)：专注记录ID（UUID，由Android端生成）

user_id(BIGINT, 索引)：用户ID（关联biz_user）

task_name(VARCHAR(100))：任务名称（如'写代码'）

duration_minutes(INT)：专注时长（分钟）

start_time(BIGINT, 索引)：专注开始时间戳（毫秒）

signature(VARCHAR(64))：防篡改签名（SHA-256）

sync_time(BIGINT)：同步到云端的时间戳（毫秒）

created_at(BIGINT)：记录创建时间戳

deleted(TINYINT)：逻辑删除标记（0=未删除，1=已删除）



三、赛博植物图鉴配置表 (biz_plant_dict)

由后台管理员动态维护的"数值策划表"。App启动时拉取此表，实现新植物上线免发版。

plant_id(INT, 主键, 自增)：植物ID（主键，自增）

plant_name(VARCHAR(50))：植物名称

description(VARCHAR(200))：植物背景故事/描述

drop_weight(INT)：盲盒掉落权重（值越大越容易掉落，稀有植物设为极低值）

resource_code(VARCHAR(100))：Android端UI贴图资源名

image_url(VARCHAR(500))：植物图片URL地址

color_hex(VARCHAR(7))：植物主色调（十六进制），默认 #00FF00

purify_range(INT)：净化辐射半径（格数）

width(INT)：植物占地尺寸（1/2/3，n×n正方形）

status(TINYINT)：状态（0=下架，1=上架）

created_at(BIGINT)：创建时间戳

updated_at(BIGINT)：更新时间戳

deleted(TINYINT)：逻辑删除标记（0=未删除，1=已删除）



四、数字资产背包表 (biz_user_bag)

记录用户每次专注掉落的种子，以及用光流解析后的成熟植物。

bag_id(CHAR(36), 主键)：背包物品ID（UUID）

user_id(BIGINT, 索引)：用户ID

plant_id(INT)：植物字典ID

status(TINYINT)：状态（0=未解析种子，1=成熟植物，2=已种植）

obtained_at(BIGINT)：获取时间戳

created_at(BIGINT)：创建时间戳

deleted(TINYINT)：逻辑删除标记（0=未删除，1=已删除）



五、赛博废土花园地块表 (biz_garden_tile)

支撑2.5D等轴测地图的云端存档，记录哪个坐标种了什么，哪些地块被净化了。

tile_id(BIGINT, 主键, 自增)：地块ID（主键，自增）

user_id(BIGINT, 索引)：地块归属人

x(INT)：2.5D坐标系的X轴

y(INT)：2.5D坐标系的Y轴

plant_id(INT, 索引)：该坐标上种植的植物ID（荒芜地块为NULL）

bag_record_id(CHAR(36))：关联的背包记录ID

deploy_time(BIGINT)：部署时间戳

last_charge_time(BIGINT)：最后一次充能时间戳（用于惰性枯萎状态机）

is_purified(TINYINT)：是否已净化（0=否，1=是）

created_at(BIGINT)：创建时间戳

updated_at(BIGINT)：更新时间戳

deleted(TINYINT)：逻辑删除标记（0=未删除，1=已删除）



六、好友关系表 (biz_friendship)

存储用户间的好友关系，支持社交系统。

id(BIGINT, 主键, 自增)：关系ID（主键，自增）

user_id(BIGINT, 索引)：发起申请的用户ID

friend_id(BIGINT, 索引)：接收申请的用户ID

status(TINYINT, 索引)：状态（0=申请中，1=已同意）

created_at(BIGINT)：创建时间戳

updated_at(BIGINT)：更新时间戳

deleted(TINYINT)：逻辑删除标记（0=未删除，1=已删除）



七、互访日志表 (biz_visit_log)

记录访客在好友花园中的互动行为（充能、留言等）。

log_id(BIGINT, 主键, 自增)：日志ID（主键，自增）

visitor_id(BIGINT, 索引)：访客ID

host_id(BIGINT, 索引)：花园主人ID

action_type(TINYINT)：互动类型（1=充能，2=留言）

content(VARCHAR(200))：留言内容（type=2时有效）

is_read(TINYINT, 索引)：是否已读（0=未读，1=已读）

created_at(BIGINT)：创建时间戳

deleted(TINYINT)：逻辑删除标记（0=未删除，1=已删除）



八、管理员表 (sys_admin)

后台管理系统管理员账户信息。

admin_id(BIGINT, 主键, 自增)：管理员ID（主键，自增）

username(VARCHAR(50), 唯一索引)：登录用户名

password(VARCHAR(64))：密码（SHA-256哈希）

nickname(VARCHAR(50))：管理员昵称

role(VARCHAR(20))：角色（默认 admin）

status(TINYINT)：状态（0=正常，1=禁用）

last_login_at(BIGINT)：最后登录时间戳

last_login_ip(VARCHAR(50))：最后登录IP

created_at(BIGINT)：创建时间戳

updated_at(BIGINT)：更新时间戳

deleted(TINYINT)：逻辑删除标记（0=未删除，1=已删除）



九、管理员登录日志表 (sys_admin_login_log)

记录管理员登录行为，用于安全审计。

log_id(BIGINT, 主键, 自增)：日志ID（主键，自增）

admin_id(BIGINT, 索引)：管理员ID

username(VARCHAR(50))：登录用户名

login_ip(VARCHAR(50))：登录IP地址

user_agent(VARCHAR(255))：浏览器User-Agent

login_result(TINYINT)：登录结果（0=失败，1=成功）

login_at(BIGINT, 索引)：登录时间戳



十、系统配置表 (sys_config)

存储系统全局配置项，支持动态配置。

config_id(BIGINT, 主键, 自增)：配置ID（主键，自增）

config_key(VARCHAR(100), 唯一索引)：配置键

config_value(TEXT)：配置值

description(VARCHAR(200))：配置说明

config_group(VARCHAR(50))：配置分组

created_at(BIGINT)：创建时间戳

updated_at(BIGINT)：更新时间戳

deleted(TINYINT)：逻辑删除标记（0=未删除，1=已删除）



设计说明



1. 主键策略：BIGINT 自增，适合高并发场景

2. 软删除：使用 deleted 字段实现逻辑删除

3. 时间戳：统一使用 BIGINT 存储毫秒级时间戳，便于跨平台同步

4. 字符集：utf8mb4 支持完整 Unicode（包括 Emoji）

5. 索引优化：针对高频查询字段建立索引



字段变更记录

2026-03-26: biz_user 表新增 last_focus_time 字段，用于统一管理花园点亮状态。用户专注完成和好友充能时都会更新该字段。
